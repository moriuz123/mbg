/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Since we migrated from Vite (ES modules), we might want to ensure compatibility
}

export default nextConfig;
